[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    live: requires the live Laravel app + sandbox tenant (skipped by default; run with `-m live`)
addopts = -ra
filterwarnings =
    ignore::DeprecationWarning
