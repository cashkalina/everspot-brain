# CHANGELOG.md — append-only audit of self-edits (§11.4)

**Purpose.** Every self-modification of the general layer (prompt / skill / knowledge /
library / contract) lands as a tracked, reversible diff and is recorded here in one line.
This is an **audit log, never read in full** — consult it on demand (e.g. "when/why did
the ask-policy change?").

**Format:** `YYYY-MM-DD · <what changed> · <one-line rationale>`

When knowledge **graduates** (§11.3 — prose → validator / default / library fn), record
the move here and **delete the prose** from the KB; this log is the trail of where it
went.

---

- 2026-06-27 · **GENERALIZATION + HONESTY PASS (items A1/A2/C1/C2/C3/B4a, ck-directed, orchestrated, uncommitted on `sandbox-flow-smoke-test`).** Offline suite **211 → 234 passed / 3 skipped**. 
  - **A1 — loader uses the atomic create+register.** `orion_load.py` now sends each NEW record's `external_id` inline in the create/`batchStore` payload (server registers it atomically via `HasExternalIds` in the same transaction) and DROPS the separate `external-ids` call for new records; the `external_id→id` map is built from the create response (`_create_payload`/`_record_created`). Kept: existing-external-ids prefetch (skip/create/PATCH), idempotent skip-unchanged, PATCH-changed, and `_repair_orphans` (now via `_register_orphans`) for crash-mid-transaction / legacy unlinked rows. One round-trip per create, no orphan window. (Closes the loader-adoption follow-up from §13.3a.)
  - **A2 — self-healing corrections.** New `reconcile.apply_corrections(project_root, snapshot, client, *, entities=None, dry_run=False)` — idempotent: re-uses the field-level live-reconcile oracle (`project_payload`/`project_location` + the `_norm` comparator) to PATCH already-loaded live records whose field VALUE drifted from the canonical projection (generalizes the manual 30-date repair + the `_correct_interment_status` special case). Wired to the CLI as `migrate reconcile --live --correct` (PATCHes, then re-reads live so the report reflects the corrected state).
  - **C2 — opaque/hashed external_ids.** `external_ids.py` mints `src:<entity>:<sha256(source_id)[:20]>` instead of embedding the raw source key — stops source PII (e.g. a decedent's name in a composite source key like `…|THOMPSON|LOIS`) leaking into the permanent id, logs, URLs, the external-ids table, and load-report errors. Deterministic + stable (upsert/v2-update stay idempotent); `mint_for` still mint-once (an existing ledger keeps its old ids; only fresh mints are opaque; a cold-start re-mint is all-opaque). 80-bit token ≈ 3e-11 collision at 10M records/entity. The only legitimate external_id parse (the `src:<entity>:` segment in `validate._ref_entity`/`contract._is_external_id`) is preserved; golden fixtures re-blessed; tests that substring-matched a source key now key off `_provenance.source_id`.
  - **C3 — never fabricate a date.** `interments.date` made **nullable** in Everspot (tenant migration `2026_06_27_000000_make_date_nullable_on_interments_table`; manual-record stage validation relaxed `date` → `nullable` so a manual+completed undated interment stays completed, non-manual app flow still requires a date). Loader composes `interment.date` from **doi/dod ONLY**, else sends `null` (removed the `1900-01-01` sentinel + the birthday fallback; `project_payload` oracle expects null so reconcile stays consistent). Contract + overlay regenerated to drop the sentinel (`interment.date` now `nullable/required_on_insert:false`, no `sentinel`); `--check` exit 0. Supersedes M3's sentinel.
  - **C1 — live LLM tier permitted on real data (user "option C").** Removed the hard PII prohibition + the `--authorize-llm` gate; `MIGRATION_LLM_DRYRUN` is now an OPTIONAL cost/determinism switch, not a safety gate (SPEC §18 rewritten; `migrate.md`, `migrate.py`, `llm_fallback.py` updated). Hard no-accidental-call safeguard: `llm_fallback.resolve_residuals` takes the deterministic offline path (never constructs a client / never calls the model) when there's no `ANTHROPIC_API_KEY` AND no explicit client — so default/CI runs can't fire a live call. Every LLM output is still re-validated through the library.
  - **B4a — configurable Everspot-codebase path.** New `scripts/config.py` `everspot_codebase_path()` resolves env `EVERSPOT_CODEBASE_PATH` → optional `pipeline.toml` → default (repo root, computed relative), plus `artisan_command()`/`has_php()`. `.claude/commands/migrate.md` now reads it for codebase-memory-mcp/grep self-introspection AND for `php artisan migration:generate-contract` (cwd = that path); documented in SPEC §16/§18 as the hook for the planned move of the pipeline into its own repository.
- 2026-06-27 · **TRUST-LAYER HARDENING PASS** (orchestrated, all test-first, all uncommitted on `sandbox-flow-smoke-test`). An adversarial five-axis review found a cluster of checks that report green while blind to the real failure mode; each is fixed behind a regression test that fails-before/passes-after. Offline suite **174 → 211 passed / 3 skipped** (+37). Per finding:
  - **H1 + L7** — count conservation was tautological (derived the "source" count from `_provenance.source_id` on the canonical records themselves → a dropped row left no record→no provenance→`dropped` pinned at 0, "✅ Conserved" printed after data loss; the manifest's true totals were never read). Fix: `assemble.py` emits a per-source-row **disposition ledger** (`runs/<v>/canonical/dispositions.json` — produced / deduped_into / skipped_out_of_scope / errored, one entry per iterated row); `validate.py`/`reconcile.py`/`report.py` now compute conservation as `accounted == manifest.total_rows` with `unexplained_dropped` BLOCKING and legitimate fan-in/dedup informational. Dropped the tautological self-vs-self offline money row (money exactness is the `--live` field-level lens's job). L7: unrecognized load actions fold into `failed`; a per-entity `failed>0` or `loaded<canonical` flips the conserved verdict to ❌. `test_regression_count_conservation_detects_drop` (drop fails; fan-in/dedup still passes; L7 fold). Bell's Chapel: PASS, accounted 2421 / unexplained 0 / deduped 2.
  - **H2** (pipeline side) — `_register` swallowed a failed `external-ids` batch yet still counted the records `created`, leaving orphan models invisible to the next run's prefetch → re-create → duplicates. Fix: an unregistered create is now counted `failed`/for-retry (not created); a pre-create **repair pass** (`_repair_orphans`) pages the live entity and adopts orphan rows by the loader's own projected-payload signature, registering them in place — so a re-run repairs rather than duplicates. `test_regression_orion_register_failure_no_duplicate`.
  - **H2** (§13.3a/b real close, Everspot repo) — entity create/`batchStore` now accept a top-level `external_id` and register it on the new model **atomically via `HasExternalIds` in the same transaction** (Customer/Property/PropertyGroup/Interment/Cemetery/ListOption, via new `modules/Common/Traits/RegistersExternalIdOnWrite.php` overriding `performStore`); per-controller `transactionsAreEnabled()=>true` pins batch atomicity for the migration path. Removes the separate round-trip and its non-atomic seam. Pest `modules/Common/Tests/Feature/Api/AtomicExternalIdRegistrationTest.php` (6, incl. mid-batch rollback leaves no orphan model). NO auth/token/guard touched.
  - **H3** — "resumable load" overclaimed: `chunks_done`/`chunk_cb` were written only on batch failure and never consulted (resume keyed off `waves_done`). Fix: removed the chunk machinery; the honest contract is **wave-level resume + idempotent upsert** (safe now that H2 makes a wave re-POST non-duplicating); `chunks_done` kept as a deprecated informational no-op for back-compat. `test_regression_orion_resume_contract_honest`.
  - **M1** — accept-all settled an unresolved `value_set` (a `proposed_answer` dict with `None` tokens was taken as answered → null cells, violating §9.2). Fix: `answer.apply_answers` rejects any value_set answer containing a `None` and keeps the question OPEN. `test_regression_acceptall_keeps_unresolved_valueset_open`.
  - **M2** — a clean v2 scoped delta FAILED validation: an in-scope child referencing an UNCHANGED (not-re-emitted) parent was flagged BLOCKING dangling_ref though the parent exists in `ledger/external_ids.json`. Fix: scoped validation (detected via `snapshots/<v>/delta.json`) seeds the ext→entity index from the external_id ledger in addition to the in-run canonical; a ref to an already-minted external_id of the right entity is satisfied (full runs still flag a genuinely-missing parent; a wrong-entity ledger hit still dangles). `test_regression_scoped_validate_ledger_ref_satisfied`.
  - **M3** — `interment.date` silently fell back to the decedent's BIRTH date (`("doi","dod","dob")`, `is_fallback=False`). Fix: dropped `dob` from the chain — doi/dod only, else the flagged `1900-01-01` sentinel (`is_fallback=True`). `test_regression_interment_date_no_dob_fallback`.
  - **M4** — wrong-cemetery silent fallback (`cems[0]` on a name miss). Fix: create the named cemetery when absent (logging a `reference_gap` when other cemeteries exist), never silently mis-assign. `test_regression_cemetery_no_silent_misassign`.
  - **M5** — `--check` gated only `target_schema.json`, so a stale derived `canonical-record.schema.json` passed CI (§6.4/§12). Fix: `GenerateMigrationContract.php --check` now derives + diffs BOTH committed artifacts and exits non-zero naming whichever drifted (write + `--check` share one `deriveCanonicalSchema()`). Offline `GenerateMigrationContractCheckTest` (drift-only-canonical case) + live `--check` exit 0 confirmed.
  - **M6** — candidate-key search was combinatorial (`combinations(cols,2..5)` × full groupby, ~10^5–10^6 scans on a wide no-early-key table). Fix (`profile.py`): top-K(15) highest-cardinality columns, distinct-product short-circuit (skip combos that can't reach 0.98×rows), a hard combo budget (2500) shared with business-key completion, and a `candidate_keys_truncated` flag when the search was bounded. Natural keys still found. `test_regression_profile_keysearch_budget`.
  - **L1** — PII samples were volume-capped, not redacted. Fix: name/phone/address/joint_name/reversed_name-signalled columns get a stable `[redacted:<sha256>]` token (sample + value-set keys); non-PII columns keep verbatim samples; honest docstring. `test_regression_profile_pii_samples_redacted`.
  - **L2** — per-phase resume-skip was advertised but no `cmd_*` called `is_done`. Fix: softened the claim — the **orchestrator** skips `status:done` phases; the `migrate <phase>` CLI re-runs deterministically; only the load self-resumes. Docstrings + `migrate.md` made honest.
  - **L3** — skipped questions bypassed the gate. Fix: a skip now REQUIRES a rationale (bare `"skip"` rejected → stays OPEN); `answer` exposes `skipped_blocking`/`gate_clear` and `migrate answer`'s exit code now gates on `gate_clear` (not just `any_open`). `test_regression_skipped_blocking_question_surfaced`.
  - **L4** — deprecated `jsonschema.RefResolver` would `AttributeError` AFTER canonical is written on a future jsonschema (the guard caught only `ImportError`). Fix: migrated `assemble.py`/`validate.py` to the `referencing` library (shared `validate._make_ref_validator`); `RefResolver` kept only as a fallback; both guards broadened to catch `AttributeError`. Installed jsonschema is 4.26.0 — **no venv pin needed** (uses `referencing`, a transitive dep). `test_regression_jsonschema_ref_without_refresolver`.
  - **L5** — discover dedupe could drop a same-id OPEN question behind an auto-resolved one. Fix: `Question.subject_key` + `_dedupe_by_id` raise a loud `ValueError` on a true id collision between distinct subjects. `test_regression_discover_qid_collision_keeps_open`.
  - **L6** — `map_draft` settled fuzzy ≥90 matches at confidence 1.0 with no gap and ignored profiler-truncated value sets. Fix: a fuzzy-only resolution keeps `is_gap=True` at confidence <1.0; a profiler-`truncated` value set emits a gap; only exact + complete value sets settle at 1.0. `test_regression_map_draft_fuzzy_and_truncated_gap`.
- 2026-06-27 · **Reverted the `CreateIntermentEventFromDoiToi` `is_manual` gate** (ck: ALL interments — historical or not — should create a calendar event). The listener is back to original; event creation is governed SOLELY by the platform setting **`interment_sync_doi_with_event`** (per user/cemetery/tenant — the on/off switch; set false to disable, no code change). `is_manual` remains the STATUS mechanism only (it does NOT suppress events). Pest test reframed: a manual interment DOES auto-create an event when the setting is on (was the inverse). Interment Pest 6 passed · pipeline 174/3. NOTE: the 910 already-migrated interments were loaded while the gate was active → they currently have NO events; new interments will. A one-time backfill (re-trigger save) would be needed for the existing 910 (only those with a complete DOI; needs a default interment calendar).
- 2026-06-27 · **Correction (§11 loop): replaced the invented `migration_mode` flag with the platform's built-in `is_manual` field.** The prior fix (below) reinvented an existing Everspot field: `is_manual` ("Manual / Enable to enter historical interment"), already wired into every `StatusConfig` — for a manual interment `CompletedConfig` uses the relaxed `getManualValidationRules()`/`getManualRequiredRelatedRecords()` (no scheduled event/space required), so `completed` is a valid terminal state and `ManageIntermentStages` never demotes it. **Removed (Interment.php):** the virtual `$isMigrating` flag, `markAsMigrating()`/`isMigrating()`, the `migration_mode` `fill()` interception, and the `manageStagesAutomatically()` short-circuit. **Kept:** `handleDefaultStatus()`'s invalid-status fallback (a genuine general improvement — a bogus status would otherwise persist and crash `StatusConfigFactory`). **Listeners:** fully reverted the invented `isMigrating()` short-circuits in `SyncIntermentEventFromDoiToi` (no-op without an interment_event), `UpdateCustomerFromInterment` (no-op on create / idempotent on the status PATCH), and `SyncMemorialPersonFromInterment` (only LINKS pre-existing memorial people, never creates — and linking is correct); `CreateIntermentEventFromDoiToi` is now gated on the platform `is_manual` field instead (a historical/manual interment must not auto-create an operational calendar event — empirically `interment_sync_doi_with_event` is ON in the seeded tenant, so a full revert would have created 910 spurious events). **Pipeline:** `orion_load.project_payload` now sets `is_manual=True` for every migrated interment (it IS a historical/manual interment) — a REAL column, so it is part of the pure projection AND reconciled; dropped the write-only `migration_mode` from `_payload`; `_correct_interment_status` now PATCHes already-loaded interments whose live `status` OR `is_manual` differs from the projection (re-applies the flag to rows loaded before this change). Proven live on bells-chapel v1: 910 interments `completed`+`is_manual=False` → 910 `completed`+`is_manual=True` (updated 910 / failed 0); reconcile field-level mismatches 0 (interment 11508 fields, customer 4665 fields — no listener corrupted data). Pest: 6 `is_manual`-based tests in `modules/Interment/Tests/Feature/IntermentStatusTest.php` (migration_mode-only tests dropped); offline suite 174 passed / 3 skipped (loader tests rewritten for `is_manual`).
- 2026-06-27 · Fixed the interment-status migration gap the field-level reconcile surfaced (910 historical burials loaded as `completed` but landing live as `awaiting-scheduling`). Root cause was Everspot-side: a state machine — the `ManageIntermentStages` listener on `IntermentSaved` auto-demotes a POSTed `completed` (no scheduled event → `CompletedConfig` validation fails → walks the status backward to `awaiting-scheduling`). **Everspot fix (Interment domain, no Orion controller/auth change):** a virtual, non-persisted `migration_mode` flag on the Interment model (set via the fill key or `markAsMigrating()`) that suppresses the operational `IntermentSaved` side-effects (auto stage-progression + event/memorial/customer sync) and lets the explicit valid status land verbatim; `handleDefaultStatus` now also rejects an invalid status (falls back to default); normal app creation is unchanged (8 Pest tests in `modules/Interment/Tests/Feature/IntermentStatusTest.php`). **Pipeline fix:** `orion_load._payload` now appends a WRITE-ONLY `migration_mode=True` to the interment payload — NOT in the pure `project_payload` (the reconcile oracle), so it never appears on a live row / is never reconciled — and a new `_correct_interment_status` step PATCHes already-loaded interments whose live status differs from the projected status (idempotent: a re-run finds none to fix, counted as skipped). Proven live on bells-chapel v1: 910 `awaiting-scheduling` → 910 `completed`, reconcile interment field mismatches 910 → 0; re-run = interment updated 0 / skipped 910. +5 tests (tests/unit/test_orion_load_interment_status.py); offline suite 173 passed / 3 skipped.
- 2026-06-27 · Added **field-level post-load reconcile** (SPEC §15.3 correctness lens, deepening §8 stage 12): `migrate reconcile --live` now proves not only that records are PRESENT live (count conservation by external_id, unchanged) but that their VALUES match. `reconcile.reconcile_live` bulk-fetches each entity resource (all columns incl. the partial-date `_year/_month/_day/_estimated` cols), the external-ids map, and — for properties — the `attribute-values` (location custom fields), in O(pages) with NO per-record calls; it then projects every canonical record the SAME way the loader writes it and diffs field-by-field against the live row. The projection is the loader's OWN oracle: refactored `orion_load._payload` to delegate to a new PURE `orion_load.project_payload(entity, rec, *, cemetery_id, property_type_id, resolve_ref)` (+ `project_location(rec)`), so load and reconcile project identically — **load behavior unchanged** (the date-sentinel warning side-effect stays in `_payload`; loader tests green). Severity is **WARN only**: value mismatches never flip the run's pass/fail (count conservation alone gates it). Surfaced in two places: `reconciliation.json` gets a `field_level` block (per-entity {records_compared, records_missing_live, fields_compared, mismatches_total} + a per-field mismatch tally that catches SYSTEMATIC drift + a capped 50/entity sample of concrete `{entity, external_id, field, expected, live}` — LOCAL artifact, never sent to an LLM), and `reconciliation.md` + the post-load reconcile section of `REPORT.md` (counts conserved + per-field tally + "N field mismatches (warn) — see reconciliation.md"). `run_state` reconcile metrics now include `field_mismatches`. +6 tests (tests/golden/test_reconcile_field_level.py — clean=0, mismatch detected+attributed+sampled+WARN, systematic per-field tally, missing-live counted, report+md render; FAKE OrionClient, synthetic only). Offline suite 167 passed / 3 skipped.
- 2026-06-27 · Built §17 structured run-state checkpointing + a resumable load. New `scripts/run_state.py` (machine-readable per-run `runs/<v>/run_state.json` — per-phase status/started/finished/metrics/outputs/error + a `load_checkpoint`; atomic tmp+rename write; injectable clock; stdlib-only) and `scripts/run_log.py` (renders the constant-format `runs/<v>/RUN_LOG.md` + UPSERTs the project-level `MIGRATION_STATUS.md`, one entry per snapshot). Every runnable stage in `migrate.py` now records its checkpoint via a thin `_phase_state` context manager (start → real metrics on success → error on failure) — state writes ONLY, no stage logic/output changed; a stage run standalone updates run_state the same as under `/migrate`. New `migrate status -s <v>` subcommand renders RUN_LOG.md + prints a compact per-phase summary; the `report` stage refreshes RUN_LOG.md + the MIGRATION_STATUS.md entry automatically. **Resumable load (the §17 item):** `orion_load.py` records wave/chunk progress into `load_checkpoint`; on a fresh `load` with an INCOMPLETE checkpoint it RESUMES (skips completed waves, finalizes on clean completion) — safe via the existing upsert-by-external_id + prefetched existing-external-ids map. `/migrate` command wired: START-OF-RUN reads run_state and skips `status:done` phases / resumes an incomplete load; END-OF-RUN produces RUN_LOG.md + MIGRATION_STATUS.md. Proven on bells-chapel v1 (run_state populated across ingest/profile/assemble/validate/reconcile/report = 6/13 done; dry-load checkpoint → all 4 waves complete; 2419 property/910 customer/910 interment). +12 tests (tests/run_state/: lifecycle, RUN_LOG/STATUS render+upsert, mocked load resume-after-crash with no double-registration); offline suite 161 passed / 3 skipped.
- 2026-06-27 · Doc-hygiene prune (follow-up): scrubbed the dangling deleted-file/two-gate references left in `scripts/` (`migrate.py`, `cleanse_runner.py`, `assemble.py`, `llm_fallback.py`, `transform_cache.py`) — docstrings now point at the runnable stages + SPEC §/`knowledge/` topics; `cmd_map`/`cmd_cleanse` no longer hand off to deleted `migrate-*.md` prompts (they point at `map-draft`/`cleanse --llm` + the `/migrate` command), dropped the dead `_ai_stage`/`_HANDOFF` helper, and replaced the "Gate-1/Gate 2" wording with the single question round · comments/help-text only, no behavior change; suite 149 passed / 3 skipped.
- 2026-06-27 · Doc-hygiene prune: removed the superseded `prompts/` (7 old two-gate stage command-specs: onboard-client, migrate-map/-cleanse-fallback/-entity-resolve/-assemble/-validate/-questions), `BUILD-KICKOFF.md` (one-time, now-done build prompt), and `scripts/README.md` (stale: deleted-doc refs, old `/migrate-*` skills, two-gate model) · all replaced by the runnable CLI stages + the single-question-round `/migrate` command + `knowledge/` + `scripts/LIBRARY.md`; fixed the resulting dangling refs in SPEC §16 (dropped the `prompts/` tree block + relocation/division-of-labor mentions) and `project-template/README.md` (operating-model/plan/Gate refs → SPEC).
- 2026-06-27 · Built the §12 test suite (tests/{unit,regression,golden,conformance}); fixed a real bug found in the process — `assemble._build_interment_table` emitted a non-contract `interment_date` field (now the contract-valid `doi` partial date), see LESSONS #8 + `test_regression_interment_doi_contract_field` · the standalone-interment path was dead against the §6 contract; the test suite caught it.
- 2026-06-27 · Created the §11 self-improvement substrate (`knowledge/` INDEX + core + 7 topics, `CHANGELOG.md`, `LESSONS.md`, `scripts/LIBRARY.md`) · foundation build, landing alongside the §6 contract and §12 test suite so every run can feed the learning loops.
- 2026-06-27 · Built §8 stage 5 (auto-draft mapping) + §9 (the single question round): new `migrate map-draft` (`scripts/map_draft.py` — deterministic per-column→contract-field drafter; resolves value-sets to real tenant ids via `resolve_list_option`, never invents; DEFERS flat-row multi-entity routing as a structural gap; never clobbers a settled ledger → `.draft` sidecar), `migrate discover` (`scripts/discover.py` — one batched pass: profile+draft+DRY assemble+validate → `questions.json`/`.md` conforming to question.schema.json+§9.4; ask-policy routing; subject-derived stable ids + idempotency vs the settled ledger; `any_open`/exit-1 orchestrator gate), and `migrate answer` (`scripts/answer.py` — accept-all or answers.json → persist `ledger/questions/<id>.json` + write back into mapping/value_sets). Reconciled `schemas/question.schema.json` to the §9.4 contract (kinds value_set|unmapped|missing_required|source_key|entity_merge|validation; handoff internal|client|either; proposed_answer mandatory). Added `contract.entity_fields`/`list_option_fields` accessors. Proven: bells-chapel `discover` over its SETTLED ledger → 0 open / 8 auto-resolved / 0 answered (idempotency, §9.5), ledger byte-identical; fresh acme_synth → map-draft confident 1:1 + value-set + gaps, discover→answer(accept-all)→assemble = 4 property/3 customer/3 interment. +19 tests (tests/discovery/); suite 134 passed / 3 skipped.
- 2026-06-27 · Made §8 stages 3/10 RUNNABLE: new `migrate profile` (`scripts/profile.py` — per-column stats, candidate keys w/ natural-key completion, value-sets, data-shape signals), and replaced the print-stub `validate`/`reconcile` with real stages (`scripts/validate.py` — schema+contract+referential-integrity+count-conservation PASS/FAIL gate; `scripts/reconcile.py` +offline conservation / `--live` Orion read). Proven on bells-chapel v1: natural key `[section,row,grave,sur_name,first_name]` @ uniqueness 1.0, 0 dangling refs, PASS, 2419/910/910 count conservation. +18 tests (tests/unit/test_profile.py, tests/golden/test_validate_reconcile.py); suite 115 passed / 3 skipped. Fixed a profile bug: bare integer columns mis-tagged as `money` (now require a money marker) + lat/long tagged `coordinate` (poor natural-key) — see test_profile regression asserts.
- 2026-06-27 · Aligned `migrate emit` output dir `runs/<v>/output/` → `runs/<v>/emit/` (migrate.py) · matches SPEC §15.1/§16 (the golden test helper already expected `emit/`); surfaced by the acceptance run as command-prose-vs-CLI drift. Suite 142 passed / 3 skipped.
- 2026-06-27 · Built §8 stage 12 (`migrate report`, `scripts/report.py`) + the `/migrate` orchestrator command (`.claude/commands/migrate.md`). `report` assembles the single consolidated `runs/<v>/REPORT.md` (§15.2) **deterministically from the run artifacts** — count conservation (source→canonical→loaded, dropped called out), what-loaded-per-entity (load/results.jsonl → canonical/load_report.json fallback → "not yet loaded"), the answered questionnaire (questions.json), data-quality flags grouped by kind (assemble_report.json), the validation PASS/FAIL line (validation_summary.json), and the post-load reconcile (reconciliation.json if `--live`); plus guarantees `runs/<v>/needs_attention.json` at the run root (§15.1, grouped by kind). Proven on bells-chapel v1: 2419/910/910 conservation · 0 open/8 auto-resolved · PASS. `.claude/commands/migrate.md` drives stages 0–13 end-to-end, pausing exactly once for the question round, with the §11.2/§11.3 self-improvement loop + the §18 binding rules embedded. +8 tests (tests/golden/test_report.py); suite 142 passed / 3 skipped.
- 2026-06-27 · Built §13.3c — first-class **Attribute-engine write path** for grave location (loader-side only, EXISTING Orion resource). `orion_load.py` now writes property section/lot/space as structured custom-field values via the idempotent `attribute-values/batch-upsert` endpoint AFTER each property is created (matched by attribute `key` → upsert-in-place, so re-runs never duplicate) — and **stops concatenating location into `description`**. The `location-property` area + its section/lot/space attributes are resolved once over the Orion read backbone (`attribute-areas`/`attributes`, like list_options) into `LoadResult.reference_gaps` if absent — ids are **never invented** (Wave-0b surfaces a gap). Added `OrionClient.post()` for the non-resource action endpoint. Proven LIVE on bells-chapel sandbox: 5 real properties written via the real loader path + READ BACK correct via Orion (written=5/failed=0/0 gaps), idempotent (AV rows stay 3 across re-runs), entity counts unchanged 2419/910/910. No Everspot/auth change needed (the sandbox already has the area + section/lot/space attribute templates wired via an advanced EA inheriting from PropertyType). Updated `knowledge/core/everspot-cheatsheet.md` + `knowledge/topics/orion-load-gotchas.md` (cleared the "location punted into description" flag). +7 tests (tests/unit/test_orion_load_attributes.py, mocked OrionClient); offline suite 149 passed / 3 skipped.
